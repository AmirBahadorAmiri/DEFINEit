<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:tools="http://schemas.android.com/tools"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="ir.DEFINEit.AppManager"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:requestLegacyExternalStorage="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.SubLearn">

        <activity
            android:name="ir.DEFINEit.views.activities.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="ir.DEFINEit.views.activities.MainActivity" />
        <activity android:name="ir.DEFINEit.views.activities.DictionaryHistoryActivity" />
        <activity android:name="ir.DEFINEit.views.activities.ShowWordActivity" />
        <activity android:name="ir.DEFINEit.views.activities.StarWordsActivity" />
        <activity android:name="ir.DEFINEit.views.activities.SettingsActivity" />
        <activity android:name="ir.DEFINEit.views.activities.TranslateHistoryActivity" />
        <activity android:name="ir.DEFINEit.views.activities.ChangeLanguageActivity" />

    </application>

</manifest>